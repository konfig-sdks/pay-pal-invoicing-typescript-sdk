import { PayPalInvoicing } from "./index";

describe("pay-pal-invoicing-typescript-sdk", () => {
    it("initialize client", async () => {
        const paypalinvoicing = new PayPalInvoicing({
        });
    });
});
